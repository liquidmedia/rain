<% content_for :page_title, cloud.title unless cloud.title.blank? %>

<%= cloud.content %>

<% if can_edit_drop?(cloud.id) %>
    [<%= link_to "Edit", "/#{cloud.name}/edit",:rel=>'#puddle',:class=>"edit" %>]
<% end %>


  <script type="text/javascript" charset="utf-8">
    jQuery(function(){
      activate_rain();
    });
  </script>

